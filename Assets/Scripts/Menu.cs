using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Menu : MonoBehaviour
{
    public enum MenuType { Default, Dialogue, Pause}
    public MenuType _currentMenuType;
}
